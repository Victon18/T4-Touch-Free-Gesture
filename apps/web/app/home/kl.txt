    <div
      className="text-white relative min-h-screen"
      style={{ backgroundColor: "#06060a", fontFamily: "'DM Mono', monospace", overflowX: "hidden" }}
    >
      <style>{`
        * { box-sizing: border-box; }
        body {
          background-color: #06060a;
          font-family: 'DM Mono', monospace;
          overflow-x: hidden;
        }
        body::before {
          content: '';
          position: fixed;
          inset: 0;
          background-image:
            linear-gradient(rgba(0, 245, 196, 0.03) 1px, transparent 1px),
            linear-gradient(90deg, rgba(0, 245, 196, 0.03) 1px, transparent 1px);
          background-size: 48px 48px;
          pointer-events: none;
          z-index: 0;
        }
        body::after {
          content: '';
          position: fixed;
          inset: 0;
          background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
          pointer-events: none;
          opacity: 0.35;
          z-index: 0;
        }
        .neon-text {
          color: #00f5c4;
          text-shadow: 0 0 20px rgba(0, 245, 196, 0.5), 0 0 60px rgba(0, 245, 196, 0.2);
        }
        .neon-border {
          border-color: rgba(0, 245, 196, 0.3);
          box-shadow: 0 0 0 1px rgba(0, 245, 196, 0.08) inset;
        }
        .card-glow:hover {
          border-color: rgba(0, 245, 196, 0.35);
          box-shadow: 0 0 40px rgba(0, 245, 196, 0.07), 0 0 0 1px rgba(0, 245, 196, 0.12) inset;
        }
        .btn-neon {
          background: #00f5c4;
          color: #06060a;
          font-family: 'Syne', sans-serif;
          font-weight: 700;
          letter-spacing: 0.04em;
          transition: all 0.25s ease;
          box-shadow: 0 0 30px rgba(0, 245, 196, 0.25);
          text-decoration: none;
          display: inline-block;
        }
        .btn-neon:hover {
          background: #fff;
          box-shadow: 0 0 50px rgba(0, 245, 196, 0.5);
          transform: translateY(-2px);
        }
        .btn-ghost {
          border: 1px solid rgba(0, 245, 196, 0.3);
          color: #00f5c4;
          font-family: 'Syne', sans-serif;
          font-weight: 700;
          letter-spacing: 0.04em;
          transition: all 0.25s ease;
          text-decoration: none;
          display: inline-block;
        }
        .btn-ghost:hover {
          background: rgba(0, 245, 196, 0.07);
          border-color: rgba(0, 245, 196, 0.6);
          transform: translateY(-2px);
        }
        .hand-ring {
          border: 1px solid rgba(0, 245, 196, 0.2);
          border-radius: 9999px;
          position: absolute;
          top: 50%; left: 50%;
          transform: translate(-50%, -50%);
          animation: ringPulse 3s ease-in-out infinite;
        }
        @keyframes ringPulse {
          0%, 100% { opacity: 0.2; transform: translate(-50%,-50%) scale(1); }
          50%       { opacity: 0.6; transform: translate(-50%,-50%) scale(1.04); }
        }
        .stagger-1 { animation: fadeUp 0.8s 0.10s ease both; }
        .stagger-2 { animation: fadeUp 0.8s 0.25s ease both; }
        .stagger-3 { animation: fadeUp 0.8s 0.40s ease both; }
        .stagger-4 { animation: fadeUp 0.8s 0.55s ease both; }
        .stagger-5 { animation: fadeUp 0.8s 0.70s ease both; }
        @keyframes fadeUp {
          0%   { opacity: 0; transform: translateY(30px); }
          100% { opacity: 1; transform: translateY(0);    }
        }
        @keyframes float {
          0%, 100% { transform: translateY(0px);  }
          50%       { transform: translateY(-18px); }
        }
        @keyframes glowPulse {
          0%, 100% { opacity: 0.4; filter: blur(40px); }
          50%       { opacity: 0.8; filter: blur(60px); }
        }
        .animate-float      { animation: float     6s ease-in-out infinite; }
        .animate-glow-pulse { animation: glowPulse 3s ease-in-out infinite; }
        nav a {
          position: relative;
          color: #5a5a72;
          transition: color 0.2s;
          text-decoration: none;
        }
        nav a::after {
          content: '';
          position: absolute;
          bottom: -3px; left: 0;
          width: 0; height: 1px;
          background: #00f5c4;
          transition: width 0.25s ease;
        }
        nav a:hover       { color: #00f5c4; }
        nav a:hover::after{ width: 100%; }
        .gesture-dot {
          display: inline-block;
          width: 6px; height: 6px;
          background: #00f5c4;
          border-radius: 50%;
          box-shadow: 0 0 8px rgba(0, 245, 196, 0.8);
        }
        .tag {
          background: rgba(0, 245, 196, 0.08);
          border: 1px solid rgba(0, 245, 196, 0.18);
          color: #00f5c4;
          font-size: 0.65rem;
          letter-spacing: 0.12em;
          padding: 3px 10px;
          border-radius: 2px;
          font-family: 'DM Mono', monospace;
          text-transform: uppercase;
        }
        .section-label {
          font-size: 0.65rem;
          letter-spacing: 0.2em;
          text-transform: uppercase;
          color: #3a3a52;
          font-family: 'DM Mono', monospace;
        }
        .stat-bar {
          height: 2px;
          background: linear-gradient(90deg, #00f5c4, #7b5ea7);
          border-radius: 1px;
          box-shadow: 0 0 8px rgba(0,245,196,0.4);
        }
        .footer-link {
          color: #3a3a52;
          text-decoration: none;
          transition: color 0.2s;
        }
        .footer-link:hover { color: #00f5c4; }
        @keyframes scanDown {
          0%   { top: -2px;  opacity: 0;   }
          10%  { opacity: 0.6; }
          90%  { opacity: 0.6; }
          100% { top: 100%; opacity: 0;   }
        }
      `}</style>

      {/* ── Ambient glows ─────────────────────────────────────────── */}
      <div
        className="fixed rounded-full pointer-events-none z-0 animate-glow-pulse"
        style={{ top: "-200px", left: "10%", width: "600px", height: "600px", backgroundColor: "#00f5c4", opacity: 0.05, filter: "blur(120px)" }}
      />
      <div
        className="fixed rounded-full pointer-events-none z-0 animate-glow-pulse"
        style={{ bottom: "-100px", right: "5%", width: "400px", height: "400px", backgroundColor: "#7b5ea7", opacity: 0.1, filter: "blur(100px)", animationDelay: "1.5s" }}
      />

      {/* ══ NAV ══════════════════════════════════════════════════════ */}
      <header className="relative z-10" style={{ borderBottom: "1px solid #1e1e2e" }}>
        <nav className="max-w-7xl mx-auto px-6 py-5 flex items-center justify-between">
          <a href="#" className="flex items-center gap-2.5">
            <span className="w-7 h-7 rounded-md flex items-center justify-center" style={{ backgroundColor: "#00f5c4" }}>
              <svg viewBox="0 0 24 24" fill="none" className="w-4 h-4" stroke="#06060a" strokeWidth="2.5">
                <path d="M7 11V7a5 5 0 0110 0v4M5 11h14l-1.5 9H6.5L5 11z" />
              </svg>
            </span>
            <span className="text-white text-lg tracking-tight" style={{ fontWeight: 800, fontFamily: "Syne, sans-serif" }}>
              Gesture<span className="neon-text">AI</span>
            </span>
          </a>
          <div className="hidden md:flex items-center gap-8 font-mono text-sm">
            <a href="#features">Features</a>
            <a href="#how">How it works</a>
            <a href="#gestures">Gestures</a>
            <a href="#pricing">Pricing</a>
          </div>
          <a href="#" className="btn-neon px-5 py-2 rounded text-sm">Get Early Access</a>
        </nav>
      </header>

      {/* ══ HERO ═════════════════════════════════════════════════════ */}
      <section className="relative z-10 max-w-7xl mx-auto px-6 pt-28 pb-24 flex flex-col lg:flex-row items-center gap-16">

        {/* Left copy */}
        <div className="flex-1 max-w-xl">
          <div className="tag mb-6 stagger-1 inline-block">AI · Computer Vision · v2.1</div>

          <h1
            className="stagger-2"
            style={{ fontFamily: "Syne, sans-serif", fontSize: "clamp(3rem,7vw,4.5rem)", fontWeight: 800, lineHeight: 1.02, letterSpacing: "-0.02em", marginBottom: "1.5rem" }}
          >
            Your Hands.<br />
            <span className="neon-text">The Interface.</span>
          </h1>

          <p className="stagger-3 font-mono max-w-md" style={{ color: "#5a5a72", fontSize: "1rem", lineHeight: "1.75", marginBottom: "2.5rem" }}>
            Real-time gesture recognition powered by on-device AI. No wearables, no controllers — just your hands and a camera. Interact with anything, from anywhere.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 stagger-4">
            <a href="#" className="btn-neon px-8 py-3.5 rounded text-sm text-center">Start for Free</a>
            <a href="#how" className="btn-ghost px-8 py-3.5 rounded text-sm text-center">Watch Demo ↓</a>
          </div>

          {/* Social proof */}
          <div className="flex items-center gap-4 mt-10 stagger-5">
            <div className="flex -space-x-2">
              {[{ l: "A", c: "#00f5c4" }, { l: "K", c: "#7b5ea7" }, { l: "R", c: "#fff" }, { l: "+", c: "#00f5c4" }].map((a) => (
                <div key={a.l} className="w-8 h-8 rounded-full border-2 flex items-center justify-center text-xs font-mono" style={{ backgroundColor: "#3a3a52", borderColor: "#06060a", color: a.c }}>
                  {a.l}
                </div>
              ))}
            </div>
            <span className="text-xs font-mono" style={{ color: "#5a5a72" }}>2,400+ developers in early access</span>
          </div>
        </div>

        {/* Right visual */}
        <div className="flex-1 flex items-center justify-center">
          <div className="relative w-80 h-80 animate-float">
            {/* Concentric rings */}
            <div className="hand-ring" style={{ width: "340px", height: "340px", animationDelay: "0s" }} />
            <div className="hand-ring" style={{ width: "280px", height: "280px", animationDelay: "0.6s" }} />
            <div className="hand-ring" style={{ width: "220px", height: "220px", animationDelay: "1.2s" }} />

            {/* Hand SVG card */}
            <div className="absolute inset-0 flex items-center justify-center">
              <div
                className="relative w-52 h-52 rounded-2xl neon-border border flex items-center justify-center"
                style={{ backgroundColor: "#12121c", boxShadow: "0 0 80px rgba(0,245,196,0.08)" }}
              >
                <svg viewBox="0 0 120 140" fill="none" xmlns="http://www.w3.org/2000/svg" className="w-28 h-28">
                  {/* Palm */}
                  <rect x="28" y="60" width="64" height="55" rx="12" fill="rgba(0,245,196,0.08)" stroke="rgba(0,245,196,0.5)" strokeWidth="1.5" />
                  {/* Fingers */}
                  <rect x="30" y="25" width="14" height="42" rx="7" fill="rgba(0,245,196,0.08)" stroke="rgba(0,245,196,0.5)" strokeWidth="1.5" />
                  <rect x="49" y="15" width="14" height="50" rx="7" fill="rgba(0,245,196,0.12)" stroke="rgba(0,245,196,0.7)" strokeWidth="1.5" />
                  <rect x="68" y="20" width="14" height="46" rx="7" fill="rgba(0,245,196,0.08)" stroke="rgba(0,245,196,0.5)" strokeWidth="1.5" />
                  <rect x="76" y="35" width="14" height="38" rx="7" fill="rgba(0,245,196,0.06)" stroke="rgba(0,245,196,0.4)" strokeWidth="1.5" />
                  {/* Thumb */}
                  <rect x="8" y="55" width="26" height="13" rx="6.5" fill="rgba(0,245,196,0.08)" stroke="rgba(0,245,196,0.4)" strokeWidth="1.5" transform="rotate(-15 8 60)" />
                  {/* Tracking dots */}
                  <circle cx="37" cy="23" r="3" fill="#00f5c4" opacity="0.9" />
                  <circle cx="56" cy="13" r="3" fill="#00f5c4" opacity="0.9" />
                  <circle cx="75" cy="18" r="3" fill="#00f5c4" opacity="0.9" />
                  <circle cx="83" cy="33" r="3" fill="#00f5c4" opacity="0.7" />
                  <circle cx="56" cy="60" r="4" fill="#00f5c4" opacity="0.3" />
                </svg>

                {/* Scan line */}
                <div className="absolute inset-0 overflow-hidden rounded-2xl pointer-events-none">
                  <div
                    className="absolute left-0 right-0 h-px opacity-60"
                    style={{ background: "linear-gradient(to right, transparent, #00f5c4, transparent)", animation: "scanDown 3s ease-in-out infinite" }}
                  />
                </div>

                {/* Status badge */}
                <div
                  className="absolute -bottom-3 left-1/2 -translate-x-1/2 flex items-center gap-2 border px-3 py-1 rounded-full text-xs font-mono whitespace-nowrap"
                  style={{ backgroundColor: "#12121c", borderColor: "#1e1e2e" }}
                >
                  <span className="gesture-dot" />
                  <span className="neon-text">21 landmarks · 60fps</span>
                </div>
              </div>
            </div>

            {/* Corner labels */}
            <div className="absolute top-2 left-6 text-xs font-mono"  style={{ color: "#3a3a52" }}>PALM_DETECT</div>
            <div className="absolute top-2 right-6 text-xs font-mono" style={{ color: "#3a3a52" }}>CONF: 0.97</div>
            <div className="absolute bottom-2 left-6 text-xs font-mono" style={{ color: "#3a3a52" }}>MODEL: v2.1</div>
            <div className="absolute bottom-2 right-6 text-xs font-mono" style={{ color: "#00c49a" }}>● LIVE</div>
          </div>
        </div>
      </section>

      {/* ══ METRICS STRIP ════════════════════════════════════════════ */}
      <section className="relative z-10" style={{ borderTop: "1px solid #1e1e2e", borderBottom: "1px solid #1e1e2e" }}>
        <div className="max-w-7xl mx-auto px-6 py-8 grid grid-cols-2 md:grid-cols-4 divide-x" style={{ borderColor: "#1e1e2e" }}>
          {[
            { val: "<8ms",  label: "Inference latency",  neon: true  },
            { val: "21",    label: "Hand landmarks",     neon: false },
            { val: "60fps", label: "Real-time tracking", neon: false },
            { val: "97.4%", label: "Detection accuracy", neon: true  },
          ].map((m) => (
            <div key={m.label} className="px-8 py-2 first:pl-0">
              <div className={m.neon ? "neon-text" : "text-white"} style={{ fontFamily: "Syne, sans-serif", fontSize: "1.875rem", fontWeight: 800 }}>
                {m.val}
              </div>
              <div className="section-label mt-1">{m.label}</div>
            </div>
          ))}
        </div>
      </section>

      {/* ══ FEATURES ═════════════════════════════════════════════════ */}
      <section id="features" className="relative z-10 max-w-7xl mx-auto px-6 py-28">
        <div className="mb-16">
          <div className="section-label mb-3">/ CAPABILITIES</div>
          <h2 style={{ fontFamily: "Syne, sans-serif", fontSize: "clamp(2rem,5vw,3rem)", fontWeight: 800 }}>
            Precision meets<br /><span className="neon-text">intelligence.</span>
          </h2>
        </div>

        <div className="grid md:grid-cols-3 gap-px" style={{ backgroundColor: "#1e1e2e" }}>
          {[
            {
              tag: "On-Device", title: "Privacy First AI",
              desc: "All inference runs locally in the browser via WebGL/WASM. No frames leave your device. No cloud round-trips required.",
              icon: <path strokeLinecap="round" strokeLinejoin="round" d="M9.75 3.104v5.714a2.25 2.25 0 01-.659 1.591L5 14.5M9.75 3.104c-.251.023-.501.05-.75.082m.75-.082a24.301 24.301 0 014.5 0m0 0v5.714c0 .597.237 1.17.659 1.591L19.8 15m-6.75-12.896c.251.023.501.05.75.082M3 12h18" />,
            },
            {
              tag: "Real-Time", title: "Sub-8ms Latency",
              desc: "Hardware-accelerated neural network inference. Gesture output at up to 60fps on modern devices, even on mobile.",
              icon: <path strokeLinecap="round" strokeLinejoin="round" d="M3.75 13.5l10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75z" />,
            },
            {
              tag: "Developer SDK", title: "Clean API Surface",
              desc: "Three lines of JavaScript to capture gestures. Full TypeScript support, React hooks, event-driven architecture.",
              icon: <path strokeLinecap="round" strokeLinejoin="round" d="M6.75 7.5l3 2.25-3 2.25m4.5 0h3m-9 8.25h13.5A2.25 2.25 0 0021 18V6a2.25 2.25 0 00-2.25-2.25H5.25A2.25 2.25 0 003 6v12a2.25 2.25 0 002.25 2.25z" />,
            },
            {
              tag: "Adaptive", title: "Custom Gestures",
              desc: "Train your own gesture vocabulary with as few as 10 examples. Fine-tune in-browser with no cloud dependency.",
              icon: <path strokeLinecap="round" strokeLinejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />,
            },
            {
              tag: "Analytics", title: "Usage Telemetry",
              desc: "Understand how users interact with your gesture UI. Heatmaps, confidence distributions, and drop-off funnels.",
              icon: <path strokeLinecap="round" strokeLinejoin="round" d="M3 13.125C3 12.504 3.504 12 4.125 12h2.25c.621 0 1.125.504 1.125 1.125v6.75C7.5 20.496 6.996 21 6.375 21h-2.25A1.125 1.125 0 013 19.875v-6.75zM9.75 8.625c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125v11.25c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V8.625zM16.5 4.125c0-.621.504-1.125 1.125-1.125h2.25C20.496 3 21 3.504 21 4.125v15.75c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V4.125z" />,
            },
            {
              tag: "Cross-Platform", title: "Works Everywhere",
              desc: "Browser, Electron, React Native, and native mobile. One SDK, every platform. WebRTC-ready for remote sessions.",
              icon: <path strokeLinecap="round" strokeLinejoin="round" d="M10.5 1.5H8.25A2.25 2.25 0 006 3.75v16.5a2.25 2.25 0 002.25 2.25h7.5A2.25 2.25 0 0018 20.25V3.75a2.25 2.25 0 00-2.25-2.25H13.5m-3 0V3h3V1.5m-3 0h3m-3 18.75h3" />,
            },
          ].map((f) => (
            <div key={f.title} className="p-8 card-glow border border-transparent transition-all duration-300" style={{ backgroundColor: "#06060a" }}>
              <div className="w-10 h-10 rounded-lg border flex items-center justify-center mb-6" style={{ backgroundColor: "#12121c", borderColor: "#1e1e2e" }}>
                <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="#00f5c4" strokeWidth="1.5">{f.icon}</svg>
              </div>
              <div className="tag mb-3">{f.tag}</div>
              <h3 className="mb-3" style={{ fontFamily: "Syne, sans-serif", fontSize: "1.25rem", fontWeight: 700 }}>{f.title}</h3>
              <p className="text-sm leading-relaxed" style={{ color: "#5a5a72" }}>{f.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* ══ HOW IT WORKS ═════════════════════════════════════════════ */}
      <section id="how" className="relative z-10" style={{ borderTop: "1px solid #1e1e2e" }}>
        <div className="max-w-7xl mx-auto px-6 py-28">
          <div className="mb-16">
            <div className="section-label mb-3">/ HOW IT WORKS</div>
            <h2 style={{ fontFamily: "Syne, sans-serif", fontSize: "clamp(2rem,5vw,3rem)", fontWeight: 800 }}>
              From camera<br /><span className="neon-text">to command.</span>
            </h2>
          </div>

          <div className="grid md:grid-cols-4 gap-6">
            {[
              { n: "01", title: "Capture",  desc: "WebRTC accesses the camera stream. Frames are processed on the GPU via WebGL at up to 60fps.", solid: true },
              { n: "02", title: "Detect",   desc: "A lightweight WASM model localises the hand bounding box and extracts 21 3D joint coordinates.", solid: false },
              { n: "03", title: "Classify", desc: "The landmark graph is passed to a classifier head trained on 50k+ labelled examples across lighting conditions.", solid: false },
              { n: "04", title: "Dispatch", desc: "A named gesture event fires on your DOM element. Handle it exactly like a click or keypress.", solid: false },
            ].map((s, i) => (
              <div key={s.n} className="relative">
                <div
                  className="w-8 h-8 rounded flex items-center justify-center font-mono font-bold text-sm mb-4"
                  style={s.solid
                    ? { backgroundColor: "#00f5c4", color: "#06060a" }
                    : { backgroundColor: "#12121c", border: "1px solid #00f5c4", color: "#00f5c4" }}
                >
                  {s.n}
                </div>
                <h4 className="font-bold text-lg mb-2" style={{ fontFamily: "Syne, sans-serif", fontWeight: 700 }}>{s.title}</h4>
                <p className="text-sm leading-relaxed" style={{ color: "#5a5a72" }}>{s.desc}</p>
                {i < 3 && (
                  <div
                    className="hidden md:block absolute top-4 right-0 w-full h-px opacity-20"
                    style={{ background: `linear-gradient(to right, ${s.solid ? "#00f5c4" : "#3a3a52"}, transparent)` }}
                  />
                )}
              </div>
            ))}
          </div>

          {/* Code snippet */}
          <div className="mt-16 rounded-xl overflow-hidden" style={{ backgroundColor: "#12121c", border: "1px solid #1e1e2e" }}>
            <div className="flex items-center justify-between px-5 py-3" style={{ borderBottom: "1px solid #1e1e2e" }}>
              <div className="flex items-center gap-2">
                {[0,1,2].map((k) => <span key={k} className="w-3 h-3 rounded-full" style={{ backgroundColor: "#3a3a52" }} />)}
              </div>
              <span className="text-xs font-mono" style={{ color: "#3a3a52" }}>gesture-example.ts</span>
              <span className="tag text-xs">TypeScript</span>
            </div>
            <div className="p-6 text-sm font-mono leading-7 overflow-x-auto">
              <div><span style={{ color: "#3a3a52" }}>{"// 1. Init"}</span></div>
              <div>
                <span style={{ color: "#7b5ea7" }}>import</span>{" "}
                <span className="text-white">{"{ GestureAI }"}</span>{" "}
                <span style={{ color: "#7b5ea7" }}>from</span>{" "}
                <span style={{ color: "#00f5c4" }}>{"'@gesture-ai/sdk'"}</span>
                <span style={{ color: "#3a3a52" }}>;</span>
              </div>
              <br />
              <div><span style={{ color: "#3a3a52" }}>{"// 2. Create instance"}</span></div>
              <div>
                <span style={{ color: "#7b5ea7" }}>const </span>
                <span className="text-white">ai </span>
                <span style={{ color: "#3a3a52" }}>=</span>
                <span style={{ color: "#7b5ea7" }}> await </span>
                <span style={{ color: "#00f5c4" }}>GestureAI</span>
                <span className="text-white">{".init({"}</span>
              </div>
              <div className="pl-6"><span className="text-white">camera: </span><span style={{ color: "#00f5c4" }}>{"'front'"}</span><span className="text-white">,</span></div>
              <div className="pl-6"><span className="text-white">fps: </span><span style={{ color: "#00c49a" }}>60</span><span className="text-white">,</span></div>
              <div className="pl-6">
                <span className="text-white">gestures: [</span>
                <span style={{ color: "#00f5c4" }}>{"'swipe'"}</span>
                <span className="text-white">, </span>
                <span style={{ color: "#00f5c4" }}>{"'pinch'"}</span>
                <span className="text-white">, </span>
                <span style={{ color: "#00f5c4" }}>{"'thumbs_up'"}</span>
                <span className="text-white">]</span>
              </div>
              <div><span className="text-white">{"});"}</span></div>
              <br />
              <div><span style={{ color: "#3a3a52" }}>{"// 3. Listen"}</span></div>
              <div>
                <span className="text-white">{"ai.on("}</span>
                <span style={{ color: "#00f5c4" }}>{"'gesture'"}</span>
                <span className="text-white">{", (event) => {"}</span>
              </div>
              <div className="pl-6">
                <span className="text-white">console.log(event.</span>
                <span style={{ color: "#00f5c4" }}>name</span>
                <span className="text-white">, event.</span>
                <span style={{ color: "#00f5c4" }}>confidence</span>
                <span className="text-white">);</span>
              </div>
              <div><span className="text-white">{"});"}</span></div>
            </div>
          </div>
        </div>
      </section>

      {/* ══ GESTURE LIBRARY ══════════════════════════════════════════ */}
      <section id="gestures" className="relative z-10" style={{ borderTop: "1px solid #1e1e2e" }}>
        <div className="max-w-7xl mx-auto px-6 py-28">
          <div className="mb-16">
            <div className="section-label mb-3">/ GESTURE LIBRARY</div>
            <h2 style={{ fontFamily: "Syne, sans-serif", fontSize: "clamp(2rem,5vw,3rem)", fontWeight: 800 }}>
              30+ built-in<br /><span className="neon-text">gestures.</span>
            </h2>
          </div>

          <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-4">
            {[
              { emoji: "👍", name: "thumbs_up", pct: "99.1%", op: 1.0 },
              { emoji: "✌️", name: "peace",     pct: "97.8%", op: 0.8 },
              { emoji: "🤏", name: "pinch",     pct: "96.3%", op: 0.75 },
              { emoji: "🖐", name: "open_palm", pct: "98.7%", op: 1.0 },
              { emoji: "👉", name: "point",     pct: "95.4%", op: 0.7 },
              { emoji: "✊", name: "fist",      pct: "98.2%", op: 1.0 },
              { emoji: "🤙", name: "call_me",   pct: "96.9%", op: 1.0 },
              { emoji: "🤞", name: "cross",     pct: "94.1%", op: 1.0 },
              { emoji: "🤘", name: "rock",      pct: "97.0%", op: 1.0 },
            ].map((g) => (
              <div
                key={g.name}
                className="rounded-xl p-5 text-center transition-all duration-200 hover:scale-105 cursor-pointer"
                style={{ backgroundColor: "#12121c", border: "1px solid #1e1e2e" }}
              >
                <div className="text-3xl mb-3">{g.emoji}</div>
                <div className="text-xs font-mono mb-1" style={{ color: "#00f5c4" }}>{g.name}</div>
                <div className="stat-bar w-full" style={{ opacity: g.op }} />
                <div className="text-xs font-mono mt-1.5" style={{ color: "#3a3a52" }}>{g.pct}</div>
              </div>
            ))}
            <div
              className="rounded-xl p-5 text-center cursor-pointer transition-all duration-200 hover:scale-105"
              style={{ backgroundColor: "#12121c", border: "1px dashed #1e1e2e" }}
            >
              <div className="text-3xl mb-3 opacity-30">＋</div>
              <div className="text-xs font-mono mb-1" style={{ color: "#3a3a52" }}>custom</div>
              <div className="text-xs font-mono mt-1.5" style={{ color: "#3a3a52" }}>Train yours</div>
            </div>
          </div>
        </div>
      </section>

      {/* ══ CTA BAND ═════════════════════════════════════════════════ */}
      <section className="relative z-10 overflow-hidden" style={{ borderTop: "1px solid #1e1e2e" }}>
        <div
          className="absolute inset-0 pointer-events-none"
          style={{ background: "linear-gradient(to bottom right, rgba(0,245,196,0.05), transparent, rgba(123,94,167,0.05))" }}
        />
        <div className="max-w-7xl mx-auto px-6 py-24 text-center relative">
          <div className="section-label mb-5">/ GET STARTED TODAY</div>
          <h2 className="mb-6 leading-tight" style={{ fontFamily: "Syne, sans-serif", fontSize: "clamp(2rem,6vw,3.75rem)", fontWeight: 800 }}>
            Ready to build<br /><span className="neon-text">gesture-native</span> apps?
          </h2>
          <p className="font-mono text-base max-w-md mx-auto mb-10" style={{ color: "#5a5a72" }}>
            Free tier includes 5,000 gesture events/month. No credit card required.
          </p>
          <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a href="#" className="btn-neon px-10 py-4 rounded text-sm">Start Building Free</a>
            <a href="#" className="btn-ghost px-10 py-4 rounded text-sm">Read the Docs</a>
          </div>
        </div>
      </section>

      {/* ══ FOOTER ═══════════════════════════════════════════════════ */}
      <footer className="relative z-10" style={{ borderTop: "1px solid #1e1e2e" }}>
        <div className="max-w-7xl mx-auto px-6 py-10 flex flex-col md:flex-row items-center justify-between gap-4">
          <div className="flex items-center gap-2.5">
            <span className="w-6 h-6 rounded flex items-center justify-center" style={{ backgroundColor: "#00f5c4" }}>
              <svg viewBox="0 0 24 24" fill="none" className="w-3.5 h-3.5" stroke="#06060a" strokeWidth="2.5">
                <path d="M7 11V7a5 5 0 0110 0v4M5 11h14l-1.5 9H6.5L5 11z" />
              </svg>
            </span>
            <span className="text-white text-sm" style={{ fontFamily: "Syne, sans-serif", fontWeight: 700 }}>
              Gesture<span className="neon-text">AI</span>
            </span>
          </div>
          <div className="flex items-center gap-6 text-xs font-mono" style={{ color: "#3a3a52" }}>
            {["Privacy", "Terms", "GitHub", "Discord", "Twitter/X"].map((l) => (
              <a key={l} href="#" className="footer-link">
                {l}
              </a>
            ))}
          </div>
          <div className="text-xs font-mono" style={{ color: "#3a3a52" }}>© 2025 GestureAI. MIT Licensed.</div>
        </div>
      </footer>

